export enum LOCAL_STORAGE_KEYS {
  savedLocale = 'saved-locale',
  theme = 'theme',
  token = 'ngStorage-token',
  user = 'ngStorage-user',
}

export const THEMES = {
  dark: 'dark',
  light: 'light',
  system: 'system',
}